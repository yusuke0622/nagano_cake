<h2>商品詳細</h2>

<div>
    <%= @item.get_image(220,160) %>
    <p>商品名<%= @item.name %></p>
    <p>商品説明<%= @item.introduction %></p>
    <p>税込価格(税抜価格)<%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</p>
</div>
    <%= link_to '編集する', edit_admin_item_path, class: 'btn btn-success' %>